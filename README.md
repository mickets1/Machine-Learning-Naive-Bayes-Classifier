# Naive Bayes Classifier in JavaScript

This repository contains an implementation of the Naive Bayes algorithm in JavaScript. Naive Bayes is a popular algorithm for classification tasks, especially in text classification and spam filtering.

## Getting Started

### Prerequisites

- Node.js installed on your machine

### Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>

2. npm install
3. npm start